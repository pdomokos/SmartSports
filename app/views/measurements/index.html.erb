<table>
    <thead>
    <tr>
        <th>username</th>
        <th>source</th>
        <th>date</th>
        <th>sys</th>
        <th>dia</th>
        <th>pulse</th>
        <th>spo2</th>
        <th colspan="3"></th>
    </tr>
    </thead>
    <% for a in @measurements %>

        <tr>
            <td><%= a.user.name %></td>
            <td><%= a.source %></td>
            <td><%= a.date %></td>
            <td><%= a.systolicbp %></td>
            <td><%= a.diastolicbp %></td>
            <td><%= a.pulse %></td>
            <td><%= a.SPO2 %></td>
        </tr>

    <% end %>
</table>